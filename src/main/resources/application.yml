spring:
  banner:
    location: classpath:banner.txt
  application:
    name: DemoSpring

  datasource:
    url: jdbc:postgresql://localhost:5432/carental
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
